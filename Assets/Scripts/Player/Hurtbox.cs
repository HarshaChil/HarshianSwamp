using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Hurtbox : MonoBehaviour
{
    /*EatingBar timeBar;

    // Start is called before the first frame update
    void Start()
    {
        timeBar = GameObject.Find("EatingBar").GetComponent<EatingBar>(); ;
    }

    // Update is called once per frame
    void Update()
    {

    }

    void OnTriggerEnter2D(Collider2D collision)
    {

        if (collision.gameObject.tag == "Enemy" && timeBar.timeLeft > 0)
        {
            float xForce = transform.position.x - collision.transform.position.x;
            collision.gameObject.GetComponent<BasicEnemy>().takeDamage();
        }
    }*/
}
